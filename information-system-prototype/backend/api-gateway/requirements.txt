# --- Web / API ---
fastapi==0.115.6
uvicorn[standard]==0.34.0

# --- Validation / settings ---
pydantic==2.10.4
pydantic-settings==2.7.1

# --- Auth (JWT validation only; no token issuing, no passwords) ---
python-jose[cryptography]==3.3.0

# --- Async reverse-proxy HTTP client ---
httpx==0.28.1
