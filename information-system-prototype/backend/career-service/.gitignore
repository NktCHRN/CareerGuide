# --- Python ---
.venv/
venv/
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# --- secrets / config (only .env.example is committed) ---
.env
*.env
!.env.example

# Note: the ESCO CSVs in data/esco/ ARE committed (free reuse under EU
# Commission Decision 2011/833/EU) — re-included via the root .gitignore.

# --- IDE / OS / logs ---
.idea/
.vscode/
*.log
.DS_Store
