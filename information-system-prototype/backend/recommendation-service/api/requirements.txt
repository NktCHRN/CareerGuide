# --- Web / API ---
fastapi==0.115.6
uvicorn[standard]==0.34.0

# --- Validation / settings ---
pydantic==2.10.4
pydantic-settings==2.7.1

# --- DB (async) + pgvector (READ-ONLY: no Alembic, the worker owns migrations) ---
SQLAlchemy[asyncio]==2.0.36
asyncpg==0.30.0
pgvector==0.3.6

# --- Auth (JWT validation only; no token issuing, no passwords) ---
python-jose[cryptography]==3.3.0

# --- Redis (cache) ---
redis==5.2.1
